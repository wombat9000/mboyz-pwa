import { BgImageDirective } from './bg-image.directive';

xdescribe('BgImageDirective', () => {
  test('should create an instance', () => {
    // const directive = new BgImageDirective();
    // expect(directive).toBeTruthy();
  });
});
